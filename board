! Knowify function returns of expressions 


* Check if the expression of a let matches
* Add _invoke to struct and module transpile
* add Self value to transpile
* TypeUsageChecker should return a Type Registry and be better named

# Add Nil type and nil value
# Real Type
# Generics
